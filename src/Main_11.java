import java.util.*;
import java.util.Scanner;

public class Main_11 {
    public static void Main(String S, String pattern){
        int answer = -1;
        for( int i =0; i<S.length(); i++){

        }

    }
}

/*

악성코드가 전산망에 침투하는지 감시하기위해 패킷에서 알파벳으로만 이루어진 문자열 S를 분리했습니다.
그 다음 문자열S에 아래 설명에 해당하는 문자열 PATTERN이 포함되어있다면 해당패킷을 악성코드 의심패킷으로 분류하려합니다.
 문자열 S에 특정 문자열 pattern이 포함되어있다면 악성코드를 전송하는 패킷으로 분류합니다.
악성코드는 pattern을 무작위로 섞은 다음 문자열 s에 숨겨 전송합니다.
따라서, 문자열 s에 재배열해서 문자열 pattern을 만들 수있는부분 문자열이 포함되어있다면 악성코드 의심 패킷으로 분류해야합니다.

먼저 문자열 S에서 재배열하여 PATTERN 을 만들 수 있느느 부분 문자열이 모두 몇 가지인지 구하려합니다.

 */